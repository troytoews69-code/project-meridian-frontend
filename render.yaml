services:
  - type: static_site
    name: women-over-40-health-app-frontend
    env: static
    buildCommand: npm install && npm run build:web
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html